import styled from "styled-components";

export const Container = styled.div`
    height: 3rem;
    width: 20rem;

    display: flex;
    gap: 1rem;
    align-items: center;

    font-family: "Roboto", sans-serif;
    font-weight: bold;
`